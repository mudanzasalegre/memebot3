# Strategy Proposals

Strategy proposals are offline artifacts. They can be generated by the tuner,
written by a human, or produced by a future analysis tool.

They never apply changes automatically. A proposal that wants live eligibility
must include `policy_replay`, `paper_forward`, and `manual_approval` in
`required_gates`, and `LIVE_CANARY_ENABLED` still controls runtime activation.
